# Shortform-Cutter Pipeline — gepinnt auf den getesteten Stand (Juli 2026)
numpy==2.5.1
soundfile==0.14.0
requests==2.34.2
rapidfuzz==3.14.5
Pillow==12.3.0          # Titel-, Pill- und Text-Layer im Shortform-Renderer
faster-whisper==1.2.1

# Windows-Terminal (ConPTY) — nur auf Windows installiert
pywinpty>=3.0.5; sys_platform == "win32"
